package com.example.mix_player.models

enum class AudioPlayerStates {
     none,
     ready,
     running,
     playing,
     bufferring,
     paused,
     complete,
     stopped,
     error,
     disposed
}
